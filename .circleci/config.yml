version: 2.1

jobs:
  build:
    resource_class: small
    docker:
      - image: ubuntu:20.04
    steps:
      - checkout
      - run: apt-get update
      - run: DEBIAN_FRONTEND=noninteractive apt-get install -y software-properties-common
      - run: add-apt-repository universe && apt-get update
      - run: DEBIAN_FRONTEND=noninteractive TZ=Europe/Brussels apt-get install -y gcc g++ cmake git doxygen libsfml-dev
      - run: mkdir -p build
      - run: cd build && cmake ..
      - run: cd build && make
      - run: cd build && make install
      - run: cd build && make doc
      - store_artifacts:
          path: build/installed
      - store_artifacts:
          path: build/doc